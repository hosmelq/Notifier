<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:notifier="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar_actionbar"
    android:background="@null"
    android:layout_width="match_parent"
    android:layout_height="?actionBarSize"
    notifier:popupTheme="@style/ActionBarPopupThemeOverlay"
    notifier:theme="@style/ActionBarThemeOverlay"
    notifier:titleTextAppearance="@style/ActionBar.TitleText" />
